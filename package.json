{
  "name": "@davna/davna",
  "private": true,
  "version": "0.1.0",
  "workspaces": {
    "packages": [
      "apps/*",
      "config/*",
      "modules/*",
      "services/*",
      "shared/*"
    ]
  },
  "scripts": {
    "add:header": "node ./scripts/add.header.js",
    "api:down": "docker compose -f ./docker-compose.yml down",
    "api:up": "docker compose -f ./docker-compose.yml up -d",
    "rm": "node ./scripts/rm.js",
    "test:api-tsc": "node scripts/api.tsc.monitor.js",
    "test:modules": "tsnd --respawn=false --no-watch scripts/run.command.ts modules test",
    "typecheck": "tsc -b --noEmit",
    "typecheck:only-files": "yarn tsc -b --noEmit --pretty false 2>&1 | grep -E \"^[^ ]+.ts\" | cut -d'(' -f1 | sort -u"
  },
  "author": {
    "name": "Yago Marinho",
    "email": "marinho.rj2@hotmail.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yagomarinho/davna.git"
  },
  "dependencies": {
    "@davna/core": "*",
    "dotenv": "^17.2.3",
    "yup": "^1.7.1"
  },
  "devDependencies": {
    "@davna/eslint": "*",
    "@davna/jest": "*",
    "@types/node": "^24.5.2",
    "esbuild": "^0.27.0",
    "rimraf": "^6.1.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5"
  },
  "license": "MIT"
}
